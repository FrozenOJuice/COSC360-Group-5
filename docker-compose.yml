services:
  backend:
    build: ./backend
    ports:
      - "3001:4000"
    env_file:
      - ./backend/.env
    environment:
      - PORT=4000

  frontend:
    build: ./frontend
    ports:
      - "4000:80"
    depends_on:
      - backend